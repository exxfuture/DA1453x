package com.dialog.thermometer.domain;

/** A user's preferred unit for displaying temperature readings; readings are stored/classified in Celsius regardless. */
public enum TemperatureUnit {
    CELSIUS, FAHRENHEIT
}
